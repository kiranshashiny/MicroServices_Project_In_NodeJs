#curl -i --request POST --header "Content-Type: application/json" --data '{"heroId": 4, "threatId": 3}' localhost:8082/assignment

# This one is to get the list of threats entirely.
curl -i localhost:8082/threats
OR URL:
#localhost:8081/heroes // works
OR
#localhost:8082/threats // works


#https://localhost:8081/heroes - does not work.

# curl -i --request POST  localhost:8082/assignment - I cannot do this for a post, 
# I will get an empty reply !

1. mongo - not required if it's already running.

2. open a new terminal 
node heroes.js 8081

node threats.js 8082


3.Open a third terminal and run the above long command and it will show the output
curl -i --request POST --header "Content-Type: application/json" --data '{"heroId": 4, "threatId": 3}' localhost:8082/assignment




2.
db.usercollection.insert({ "username" : "testuser1", "email" : "testuser1@testdomain.com" })
3.
db.usercollection.find().pretty()
